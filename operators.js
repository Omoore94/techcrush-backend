// ARITHMIC OPERATORS => BODMAS (+ modulo %).. This is basic maths.
let x = 15;
let y = 19;
z = x + y;

console.log(z);

let a = "20"; //string.
let b = 10;
// addition of a string and a number performs concatenation/joining. 
// the + symbol is used to concatenate/join string and number. 
// arithmetic operators will always return a number data type.
console.log(a + b);

let p = 10;
let q = 5;
let r = p + q;
let s = p - q;
let t = p / q;
let u = p % q; //the modulo operator (%) divides the arithmic and returns the remainder from the operation. 
let v = p * q;
console.log(r);
console.log(s);
console.log(t);
console.log(u);
console.log(v);

//COMPARISON OPERATORS:
// This is used to compare two or more values.
//comparison operators always return booolean data type. 
// == strictly equal to'
// !== not strictly equal to.
// > greater than
// < less than

let c = p == q;
let d = p !== q;
let e = p > q;
let f = p < q;

console.log(c);
console.log(d);
console.log(e);
console.log(f);

// LOGICAL OPERATORS (AND, OR, NOT)
// && (AND) => Both conditions must be true
// || (OR) => Atleast one condition must be true
// ! (NOT) => Flips true to false and false to true. 



// ASSIGNMENT OPERARTORS
// (=) EQUAL TO -> for assigment
// += adds and reassigns.
// -= subtract and reassigns.

let i = 50;
let j = i += 5; //add 5 to i and re-assign. 
let k = i -= 10;  //subtract 10 from i and re-assign.
let m = i *= 2;  //multiply i by 2 and reassign.
let n = i /= 5;

console.log(i);
console.log(j);
console.log(k);
console.loh(m);
console.log(n);